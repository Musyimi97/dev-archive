# ledger-api

Internal payments ledger. Records transfers, webhook receipts, and settlement batches.

Do not load this whole service into an agent. The vault Overview + Signals notes are enough to find the route.
